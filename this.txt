-> react.js is a one javascript library . tema teno use user interface mate thay che
-> userinterface etle jene aapde joi sakiye chiye ane tene use kari sakiye chiye
-> tena amuk elements hoi che jema kam thay che button,text,image,icon,button,menu,input box,form,checkbox,dropdown,slider,navbar,slidebar,links
 tabs 

 website ma user inteface su use thay che

 - home page login
 - login form
 - product cards
 - search bar

 app ma user interface

 - touch screen
 - swipe,top
 - icons, layouts

 main feactures

 - app-jsx component - based architecture
 - virtual dom for fast rendering
 - declarative userinterface
 - resusable code

 react nu second name - SPA = single page appilcation
 
 react nu ek folder banavanu

 == npx create-react-app my-app
 tema node modules public src package json badhu ja aave


 🌍 public

Ye direct browser me load hota hai

Isme hota hai:

index.html
images
favicon

👉 Browser sabse pehle yahin se React app start karta hai

⚛️ src (MOST IMPORTANT ❤️)

💯 Real React yahin likhte ho

Isme hota hai:

App.js / App.jsx → main component
index.js / main.jsx → React start point
CSS files
Components

👉 90% kaam src ke andar hota hai

        Numbers
        objects
        bolleans - true or false
        String
        undefined
        null

 ****************************   HOOKS    **************************** 

        1..usestate :-

        value change thay tyare
        // state value store karva mate

        const [state, setState] = useState(initialValue);
                 
               state       updated 
              variable    function
        example:-
        const [count, setCount] = useState(0);

        2..useEffect :- 
        Jab component load ho 
        sideeffects/lifecycle

        useEffect(() => {
        // code
         }, [dependency]);
      
        ex:-

           (() => {
        console.log("Component loaded");
        }, []);

        4..usecontext :-

    👉 Global data use karne ke liye  

        const value = useContext(MyContext);
            ex:-
        const user = useContext(UserContext);

        5..useRef :-
        DOM access / value store (no re-render)

        const ref = useRef(initialValue);

        example:- 
        const inputRef = useRef();

        6..useReducer
        
        jab bahut saari state ho
        rules ke hisaab se change karni ho

        👉 Complex state handle

        const [state, dispatch] = useReducer(reducer, initialState);
        
        example:-
        const [state, dispatch] = useReducer(myReducer, { count: 0 });

        7..usecallback
        function bar-bar na bane
        function ko memoize karna

        const memoFn = useCallback(() => {
        // function code
        }, [dependency]);

        ex:-
          const handleClick = useCallback(() => {
          console.log("Clicked");
          }, []);

        8..useLayoutEffect

         useLayoutEffect(() => {
         // code
           }, []);
         
        9..useImperativeHandle


        jsx= javascript with xml -jsx

        jsx expressions :-

        jyare aapde kaik component banavyo hoy tyare tene new file ma import karvanu and functions ma return kari ne lakhi ne 
        index.html ma click karvanu etle ee jsx expressions

        jsx attribuets :-

        click,dbclick,mouseup,evnt....

        class Components:-

        1️⃣ Class – component define karta hai
        2️⃣ State – component ka data store karta hai



****************************react router*****************************************

- -> Router React me wo system hai jo URL ke according page (component) change karta hai bina page reload kiye.

Normal website ma page change karo to browser new page load kare.
React ma page reload thaya vagar screen change thay – aa kaam React Router kare.

******************************lifecycle**************************************

1. Mounting phase

- intial setup
- setup
- api call

2. Updating phase

-state change ho
-props change ho

work 
 - ui update
 - data change handle

3. unmounting phase

- Screen se remove ho jata hai

work:-
cleanup
timer stop
event remove

👉 useState = Data store karva mate

Value ne store kare

Value change thay to component re-render thay

UI update thay

useEffect :------

API call
Timer
Console log
DOM change
Event listener

class component 

class MyComponent extends React.Component {
   render() {
      return <h1>Hello</h1>
   }
}
 
functional component

function MyComponent() {
   return <h1>Hello</h1> 
}

Point	   Functional	   Class
Syntax     	function	          class
State	     useState	    this.state
Effect     	useEffect	  lifecycle methods
this      keyword          	Nathi	Jaruri
Code      Length	          Short	Thodu lambo
Modern React     	✅ Use thay che	          ❌ Rare use








